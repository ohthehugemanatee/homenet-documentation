---
# Monkeyble harness for the tls_cert role only (#364).
  hosts: shoebox
  gather_facts: false

  vars:
    k8s_kubeconfig: "{{ lookup('env', 'KUBECONFIG') or (lookup('env', 'HOME') + '/.kube/config') }}"

  roles:
    - tls_cert
