# Plaintext dummy credentials for CI testing, same status as test_secrets.yml.
# The alert task that reads them is mocked in the scenario, so nothing is POSTed.
PUSHOVER_TOKEN=ci-fake-app-token
PUSHOVER_USER=ci-fake-user-key
