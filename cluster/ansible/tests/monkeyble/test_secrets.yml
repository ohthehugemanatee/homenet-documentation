---
# Plaintext dummy secrets for CI testing.
# These values are safe to commit — they protect nothing real.
# All tasks that use these vars are mocked in Monkeyble scenarios,
# so the values are never sent to Pushover or used to join a real cluster.
k3s_token: ci-test-token
pushover_app_token: ci-fake-app-token
pushover_user_key: ci-fake-user-key
