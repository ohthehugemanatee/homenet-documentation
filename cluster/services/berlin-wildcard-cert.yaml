apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: berlin-wildcard
  namespace: kube-system
spec:
  secretName: berlin-vertesi-com-wildcard-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - "*.berlin.vertesi.com"
